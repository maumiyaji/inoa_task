Desafio INOA

1. Montar o protótipo no Figma
2. Pesquisar os ativos e montar um json com os dados
3. Criar os botões e campos de entrada (Montar o menu em REACT)
4. Criar o script para buscar os dados, baseado na entrada
5. Usar o Chart.JS para criar o gráfico

No JSON, para cada ativo: nome (VALE3), cor, key, array com os valores

-> Criar a função para pegar o nome ou id do ativo clicado e guardar em uma variável. OK

-> Essa variável vai ser usada para
	• criar a legenda do gráfico
	• buscar os dados para o gráfico (json)
	• criar um array com os ativos escolhidos

-> criar um botão para "apagar" o ativo escolhido da array, retirando do gráfico também!

-> A função para o gráfico vai buscar a array com o ativos escolhidos e, daí, buscar os dados no json


--------------- CRIAR UMA FUNÇÃO PARA ENCONTRAR AS DATAS NO JSON E CRIAR UMA NOVA ARRAY, "SLICEANDO" A PARTE ESCOLHIDA

--------------- ENCONTRE O INDEX DE CADA DATA E DEPOIS CRIE O SLICE


1. Pedir as datas
2. Pedir os ativos
3. 


  /* const lineChartData = {
          labels: chartData.map((data) => data.label),
          datasets: [
            {
              label: items[0],
              data: chartData.map((data) => data.revenue[numbero].VAL),
              tension: .3,
              backgroundColor: "#064FF0",
              borderColor: "#064FF0"
            },
            {
              label: items[1],
              data: chartData.map((data) => data.revenue[1].VAL),
              tension: .3,
              backgroundColor: "#06AFF0",
              borderColor: "#06AFF0"
            },
            {
              label: items[2],
              data: chartData.map((data) => data.revenue[2].VAL),
              tension: .3,
              backgroundColor: "#b80000",
              borderColor: "#b80000"
            },
          ]
        }; */